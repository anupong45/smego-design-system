# SME.GO Design Principles

**หลักการตัดสินใจด้านการออกแบบ** · ใช้ข้ามทุกชั้น 01–07 · คู่กับ [`STYLE-GUIDELINE.md`](STYLE-GUIDELINE.md)

---

## 0 · เอกสารนี้มีไว้ทำอะไร

`STYLE-GUIDELINE.md` ตอบว่า **เขียนอย่างไร** · เอกสารนี้ตอบว่า **เลือกอะไรเมื่อสองสิ่งที่ถูกต้องขัดกันเอง**

ปัญหาที่มันแก้: ตอนสร้างชั้น 04 Marketplace Pattern และ 05 Templates จะเจอคำถามแบบ *"ค่าส่งที่ยังคำนวณไม่ได้ ซ่อนใน accordion ก่อนได้ไหม"* ซึ่ง `01-brand.md` ตอบไม่ได้เพราะขอบเขตเป็นเรื่องแบรนด์ และ `theme.css` ตอบไม่ได้เพราะเป็นเรื่อง lint

**โครงสร้าง 2 ชั้น**

| ชั้น | จำนวน | กติกา |
|---|---|---|
| **A · Constraints** | 5 | ไม่เรียงลำดับ · **ไม่แพ้ข้อใด** · ละเมิดแล้วไม่ merge |
| **B · Principles** | 4 | **เรียงแพ้ชนะ** — เมื่อขัดกัน ข้อที่เลขน้อยกว่าชนะ |

⚠️ **ทำไมต้องแยก 2 ชั้น** — ถ้าเอา accessibility ไปเรียงรวมกับ *"ตัดสินใจได้เร็ว"* จะมีคนอ้างว่าข้อบนชนะได้ ชั้น A จึงอยู่นอกระบบลำดับโดยเจตนา ไม่ใช่เพราะสำคัญกว่า แต่เพราะ**มันไม่ใช่ของที่เอาไปแลก**

---

# ชั้น A · Constraints

---

## A1 · เข้าถึงได้โดยปริยาย
*Accessibility by Default*

**หลัก** — WCAG 2.2 AA เป็นเกณฑ์ผ่าน/ไม่ผ่าน component ที่ยังไม่ผ่านคือ component ที่ยังไม่เสร็จ

**ทำไม** — เจ้าของ SME ไทยจำนวนมากอายุ 40–60 ปี สายตายาว ใช้ Android ราคาประหยัดกลางแดด และแพลตฟอร์มภาครัฐมักมีข้อผูกพันตามกฎหมายด้วย ไม่ใช่แค่ความตั้งใจดี

**เกณฑ์ตรวจ**

- ตัวอักษร ≥ **4.5:1** · ไม่ใช่ตัวอักษร ≥ **3:1** (SC 1.4.11) — ทุกคู่สีมี**ตัวเลขที่วัดจริง**ในเอกสาร ไม่ใช่ "ดูแล้วน่าจะผ่าน"
- ใช้งานครบด้วยคีย์บอร์ดล้วน · `:focus-visible` เท่านั้น · ไม่มีกับดัก focus (SC 2.1.2)
- เป้ากด ≥ **24×24 CSS px** (SC 2.5.8) — ดู `01-brand.md §5.3` สำหรับขอบเขตที่แท้จริงของข้อยกเว้น *Spacing*
- ไม่มี horizontal scroll ที่ **320px** และที่ตัวอักษร **200%** (SC 1.4.10)
- สีอย่างเดียวห้ามสื่อความหมาย — ทุกสถานะมีรูปทรงหรือข้อความกำกับ
- `jest-axe` ผ่านทุก component · รายละเอียดครบใน [`01-foundations/10-accessibility.md`](01-foundations/10-accessibility.md)

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| สัดส่วนร่วมจ่ายใน tooltip | ใน `meta` ของการ์ด | ผู้ใช้ touch เข้าไม่ถึง (SC 1.4.13) — `GrantCard.md §9` |
| `display: none` บน file input | `sr-only` | focus ไปไม่ถึง กดด้วยคีย์บอร์ดไม่ได้ — `Payment.md §9` |
| ยอดเงินอยู่ในภาพ QR อย่างเดียว | ข้อความข้างนอกด้วย | ผู้ใช้ที่มองไม่เห็นสแกนจอตัวเองไม่ได้ — `Payment.md §9` |
| ปิด `Esc` ด้วย `preventDefault` | ปล่อยให้ทำงาน | ไม่ผ่าน SC 2.1.2 — `Dialog.md §9` |
| `<div>` เป็นหัวข้อ dialog | `<Heading slot="title">` | ไม่มี `aria-labelledby` = dialog ไม่มีชื่อ |

**เมื่อขัดแย้ง** — ไม่แพ้ข้อใด

---

## A2 · ตอบสนองทุกจอ และมือถือทำได้ครบ
*Responsive by Default, Mobile-Complete*

**หลัก** — **ห้ามมีงานใดที่ทำได้บนเดสก์ท็อปแต่ทำไม่ได้บนมือถือ** ส่วนจะเริ่มวาดจากจอไหนก่อนเป็นเรื่องของงานนั้น

**ทำไม** — ผู้ซื้อ SME เปิดจากมือถือระหว่างวัน ส่วนงานเปรียบเทียบผู้ขายและ dashboard ผู้ขายเกิดบนจอกว้างเป็นหลัก การประกาศ *"mobile first"* ทั้งที่ทีมออกแบบตารางเปรียบเทียบจากจอ 1280 จริง ๆ จะทำให้หลักการข้อนี้ถูกละเมิดทุกวันจนไม่มีใครเชื่อเอกสารทั้งฉบับ

> 📌 **แก้ความแม่นยำ** — เดิมข้อนี้เขียนว่า *"Mobile First / 360px มาก่อน"* (`01-brand.md §4.6` ฉบับแรก)
>
> ข้อความเดิมสั่ง **ลำดับการทำงาน** ซึ่งบังคับไม่ได้และตรวจไม่ได้ · ข้อความใหม่สั่ง **ผลลัพธ์** ซึ่งตรวจได้ด้วยคำถามเดียว: *"มีงานไหนที่มือถือทำไม่ได้บ้าง"*
>
> หลักฐานว่าของเดิมไม่ตรงกับงานจริง: `Compare.md §3` ตัดสินใจ **สลับเป็นแนวตั้งที่ 320px** โดยออกแบบจากตารางเดสก์ท็อปแล้วแปลงลง — ซึ่งเป็นการตัดสินใจที่ถูกต้อง แต่ผิด "360px มาก่อน" ตามตัวอักษร

**เกณฑ์ตรวจ**

- ทุกงานที่ทำบนเดสก์ท็อปได้ ต้องมีเส้นทางบนมือถือ — **ห้ามข้อความ "กรุณาใช้คอมพิวเตอร์"** ไม่ว่ากรณีใด
- ทดสอบที่ **320px** และ **1280px** ทุกครั้ง · breakpoint ตาม [`01-foundations/08-breakpoints.md`](01-foundations/08-breakpoints.md)
- เมื่อ layout เปลี่ยนตามจอ **ต้องประกาศ role ทุกตัว** — Safari/VoiceOver ทิ้ง semantic ของตารางเมื่อ `display` ไม่ใช่ `table` (`Compare.md §5`)
- แถบ fixed ต้องประกาศ **ตัวแปรความสูงของตัวเอง** (`--compare-bar-height` ฯลฯ) จากค่าที่วัดได้ · ชั้น token รวมเป็น `--bottom-inset` แล้วจองพื้นที่ให้ — **ห้าม**เขียนตัวแปรของแถบอื่นหรือ `body.style` เพราะ last-writer-wins เมื่อมีสองแถบ (`Compare.md §5`)
- Figma ต้องมี frame ของจอแคบจริง ไม่ใช่แค่ย่อ — `Checkout.md §7`: *"`Mobile (text)` frame ต้องมีจริง"*

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| เลื่อนตารางเปรียบเทียบแนวนอนที่ 320px | สลับเป็นแนวตั้ง | เห็นสองค่าพร้อมกันไม่ได้ = เทียบไม่ได้ — `Compare.md §9` |
| วงกลม stepper 5 อันที่ 320px | ข้อความ `2/4 ที่อยู่จัดส่ง` | ที่ 320px ได้วงกลมละ 40px **ไม่มีที่ให้ป้ายชื่อเลย** — `Checkout.md §3` |
| ปุ่มหลักอยู่ล่างบนมือถือ | `flex-col-reverse` | ปุ่มหลักต้องอยู่ในระยะนิ้วโป้ง (ข้อ 08 §7) — `Dialog.md §9` |
| ออกแบบตารางเดสก์ท็อปแล้วไม่ทำเส้นทางมือถือ | ทำทั้งสอง | ผิดข้อนี้ · การเริ่มจากเดสก์ท็อป**ไม่ผิด** |
| ทดสอบแค่ชื่อสินค้าสั้น | ทดสอบชื่อ 60+ ตัวอักษร | ชื่อสินค้าไทยยาวและถูก clamp 2 บรรทัด — `ProductCard.md §9` |

**เมื่อขัดแย้ง** — ไม่แพ้ข้อใด

---

## A3 · ความน่าเชื่อถือจากความสม่ำเสมอ
*Trust through Consistency*

**หลัก** — component เดิมทำงานเหมือนเดิมทุกที่ · ทุกค่ามาจาก token ถ้ามีค่าที่ token ไม่ครอบคลุม แปลว่า **token ผิด** แก้ที่ token ไม่ใช่ hardcode ทับ

**ทำไม** — ผู้ใช้ที่กำลังกรอกเอกสารขอสินเชื่อเรียนรู้ระบบจากหน้าแรกแล้วคาดหวังว่าหน้าที่สองทำงานเหมือนกัน ความไม่สม่ำเสมอในระบบราชการอ่านได้เป็น "ระบบนี้ทำครึ่ง ๆ กลาง ๆ" ซึ่งแปลตรงเป็นการไม่กรอกเลขบัญชี

**เกณฑ์ตรวจ**

- `node 02-tokens/validate-tokens.js` ผ่านครบ **11 ข้อ** · token ไม่ drift
- 11-rule lint contract ใน [`02-tokens/theme.css`](02-tokens/theme.css) ผ่าน — ห้าม `shadow-{xs..2xl}` · `border-neutral-*` บน input · `text-white` บนทอง · `!important` · `rounded-full` บนปุ่ม · `italic`/`uppercase`/`tracking-*` บนข้อความไทย · เลขไทย ๐–๙ ในตาราง
- ไม่มี HEX, px, ms, `cubic-bezier()` เขียนตรงใน component
- ลำดับตัวเลือกที่มีนัยต่อผู้ใช้ **ตายตัวใน component** ไม่ปล่อยให้ผู้เรียกส่งเอง
- `.md` ครบ 9 หัวข้อเรียงลำดับเดียวกัน (`STYLE-GUIDELINE.md §6`)

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| ให้ผู้เรียกส่ง `methods` เอง | ลำดับตายตัวใน component | drift ระหว่างหน้า — `Payment.md §9` |
| ตัวเลขไม่มี `tabular-nums` | มี | หลักไม่ตรง → อ่านผิดหลัก — `Checkout.md §9` |
| ทศนิยมบ้างไม่มีบ้าง | `minimumFractionDigits: 2` | คอลัมน์เงินไม่ตรงแนว |
| เลขไทย ๐–๙ ในคอลัมน์ราคา | เลขอารบิก | กว้างต่างกัน **36.6% em** — `ProductCard.md §9` |
| `<Badge variant="accent">` สำหรับ "สินค้าหมด" | `neutral` | ทองห้ามเป็นสถานะ (ข้อ 02 §9) |

**เมื่อขัดแย้ง** — ไม่แพ้ข้อใด

---

## A4 · เชื่อถือได้ระดับงานราชการ
*Government-grade Reliability*

**หลัก** — สองข้อ: **(ก)** ทุก state ถูกออกแบบไว้ ไม่มีทางตันที่ผู้ใช้แก้เองไม่ได้ · **(ข)** การกระทำที่ย้อนกลับไม่ได้ต้องยืนยันโดยแสดงสิ่งที่กำลังจะเกิดเป็นชื่อและตัวเลขจริง

**ทำไม** — ที่นี่ผู้ใช้ตัดสินใจเรื่องที่มีผลต่อธุรกิจจริง — จะกู้เงินไหม จะโอน 1,337,500.00 บาทไหม ปุ่มที่กดแล้วเงียบ 3 วินาทีเพราะไม่มี loading state ทำให้คนกดซ้ำ และการกดซ้ำในหน้าชำระเงินคือความเสียหายทางการเงินจริง

⚠️ ข้อนี้เคยเป็นคำโฆษณา — *"government-grade"* ที่ไม่มีเกณฑ์จะกลายเป็นข้ออ้างสารพัดประโยชน์ เกณฑ์ 2 ข้อด้านล่างจึงเป็น**นิยามทั้งหมด**ของคำนี้ ไม่มีความหมายอื่นแอบอยู่

**เกณฑ์ตรวจ — (ก) ครบทุก state**

- §4 ของเอกสาร component ต้องแจง **8 state**: default · hover · focus-visible · active · disabled · loading · error · empty
- **ยกเว้น component ที่แสดงผลล้วน** — ประกาศชัดเจนว่าไม่มี state เช่น `Deadline.md §4`: *"`DeadlineBadge` และ `DeadlineText` ไม่มี state — เป็นการแสดงผลล้วน"* · การประกาศคือข้อกำหนด การเว้นว่างไม่ใช่
- component ที่สืบทอด state ต้อง**ระบุว่าสืบทอดจากไหนและครบข้อใดบ้าง** ไม่ใช่ชี้ไปเฉย ๆ
- ทุก async action มีทั้งเส้นทาง loading และ error · error ที่ผู้ใช้ต้องทำใหม่ทันทีใช้ `role="alert"` ไม่ใช่ `aria-live="polite"`
- ข้อความ error ตามสูตร **อะไรผิด → ทำไม → แก้อย่างไร**

**เกณฑ์ตรวจ — (ข) ย้อนกลับไม่ได้ต้องยืนยันด้วยข้อมูลจริง**

- คำยืนยันต้องมีชื่อหรือตัวเลขจริง ไม่ใช่ *"รายการนี้"* — และบอกผลที่ตามมา (`Dialog.md §8`: *"การลบนี้ย้อนกลับไม่ได้"*)
- ปุ่มทำลายต้องให้ผู้ใช้คีย์บอร์ดเจอ **"ยกเลิก" ก่อน "ลบถาวร"** — เป็นพฤติกรรมที่ต้องการ (`Dialog.md §5`)
- **ห้าม optimistic UI กับเงิน** — ยอด สถานะการชำระ และวงเงิน แสดงหลังยืนยันจากเซิร์ฟเวอร์เท่านั้น
- จำนวนที่ยังไม่รู้ค่าใช้ `null` ไม่ใช่ `0` · ฟอร์มยาวบันทึกร่างอัตโนมัติ (ข้อ 01 §4.4)

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| `shipping={0}` ตอนยังไม่รู้ค่าส่ง | `null` | `0.00` บอกว่าฟรี ซึ่งอาจไม่จริง — `Checkout.md §9` |
| `price={0}` แทนการขอใบเสนอราคา | `price={null}` | "0 บาท" อ่านว่าฟรี — `ProductCard.md §9` |
| `vat` เป็น optional | บังคับเสมอ | ไม่แสดง ≠ ยกเว้นภาษี — `Checkout.md §9` |
| `coPaymentPercent` เป็น optional | บังคับเสมอ | ไม่แสดง ≠ 0% — `GrantCard.md §9` |
| error ใช้ `aria-live="polite"` | `role="alert"` | ผู้ใช้ต้องทำใหม่ทันที — `Payment.md §9` |
| QR หมดอายุแล้วรีเฟรชเอง | ปุ่มขอใหม่ | SC 2.2.1 — `Payment.md §9` |
| ปุ่มยืนยันชำระเงินไม่มี loading state | มี · และกดซ้ำไม่ได้ระหว่างรอ | กดซ้ำ = จ่ายซ้ำ |

**เมื่อขัดแย้ง** — ไม่แพ้ข้อใด

---

## A5 · เร็วบนเครื่องที่ช้า
*Fast on Slow Devices*

**หลัก** — เกณฑ์อ้างอิงคือ **Android ระดับล่าง บนเน็ต 4G ต่างจังหวัด** ไม่ใช่ MacBook บน WiFi ออฟฟิศ

**ทำไม** — กลุ่มผู้ใช้จริงของแพลตฟอร์มนี้อยู่บนเครื่องกว้าง 360px ที่ราคาต่ำกว่าหมื่นบาท เอฟเฟกต์ที่ "แค่หน่วงนิดเดียว" บน M-series คือกระตุกเห็นได้ชัดที่นั่น

**เกณฑ์ตรวจ**

- ฟอนต์ **2 family เท่านั้น** (Anuphan + IBM Plex Mono) โหลด subset ไทย+ละติน
- **ห้าม `backdrop-filter`** และเงาซ้อนหลายชั้น — โดยเฉพาะในรายการยาว (ข้อ 07 §5.1)
- ห้ามใช้ JavaScript ทำ animation ที่ CSS ทำได้ · Framer Motion เฉพาะ exit / layout transition / gesture
- animation ที่เปลี่ยน layout ใช้ `duration-medium` (250ms) ไม่ใช่ `fast` (150ms) — 150ms เร็วเกินไปสำหรับการเปลี่ยน layout
- ความยาว transition ต้องไม่ผูกกับขนาดเนื้อหา — `grid-template-rows: 0fr → 1fr` ไม่ใช่ `max-height: 1000px`
- เคารพ `prefers-reduced-motion` ทั้ง media query และ `useReducedMotion()`

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| `backdrop-blur-sm` บน dialog backdrop | opacity ล้วน | `backdrop-filter` แพงเกินไปบน Android ระดับล่าง — `Dialog.md §9` |
| `max-height: 1000px` เปิด/ปิด accordion | `grid-template-rows: 0fr → 1fr` | ความเร็วไม่คงที่ — สั้นดูช้า ยาวดูกระตุก — `Accordion.md §9` |
| ไม่กรอง `e.propertyName` ใน `transitionend` | กรอง `'grid-template-rows'` | handler ทำงานหลายรอบต่อการเปิดหนึ่งครั้ง |
| animation ตัวเลขแบบ count-up | แสดงค่าตรง ๆ | ดูเหมือนตัวเลขไม่นิ่ง — และเสียเฟรมฟรี |
| `useEffect` ตั้งความสูงแถบ | `useLayoutEffect` | มีหนึ่งเฟรมที่ค่ายังผิด — `Compare.md §9` |

**เมื่อขัดแย้ง** — ไม่แพ้ข้อใด

---

# ชั้น B · Principles — เรียงแพ้ชนะ

**กติกา: เมื่อสองข้อขัดกัน ข้อที่เลขน้อยกว่าชนะ** และการชนะต้องเขียนเหตุผลไว้ใน §9 ของ component นั้น

---

## B1 · ตัดสินใจได้เร็ว
*Fast Decision Making*

**หลัก** — **ทุกอย่างที่จำเป็นต่อการตัดสินใจซื้อ อนุมัติ หรือสมัคร ต้องเห็นโดยไม่ต้องคลิก**

**ทำไม** — ผู้ซื้อ B2B ไม่ได้เลือกของเล่น เขากำลังตอบคำถามเดียวว่า *"สั่งเจ้านี้ได้ไหม"* ข้อมูลที่เปลี่ยนคำตอบนั้นแล้วอยู่หลังคลิก แปลว่าคนส่วนใหญ่ตัดสินใจด้วยข้อมูลไม่ครบ หรือเสียเวลาแล้วค่อยรู้ว่าไม่เข้าเกณฑ์

**เกณฑ์ตรวจ**

ทดสอบด้วยคำถาม: *"ถ้าค่านี้เปลี่ยน คำตอบว่าซื้อ/สมัครไหม เปลี่ยนด้วยหรือไม่"* — ถ้าใช่ **ห้ามซ่อน** ห้ามอยู่ใน tooltip ห้ามอยู่ในหน้ารายละเอียดอย่างเดียว

รายการที่ตัดสินไปแล้วว่าเข้าข่าย

| ข้อมูล | ต้องอยู่ที่ | อ้างอิง |
|---|---|---|
| MOQ | บนการ์ด ไม่ใช่หน้ารายละเอียด | `ProductCard.md §5` |
| สัดส่วนร่วมจ่าย | ใน `meta` ของการ์ด · `coPaymentPercent` บังคับ | `GrantCard.md §5/§9` |
| VAT แยกบรรทัด | ทุก variant — **ไม่มี property `Show VAT`** | `Checkout.md §7` |
| ค่าส่ง | บรรทัดของตัวเอง · `null` เมื่อยังไม่รู้ | `Checkout.md §9` |
| วันปิดรับสมัคร | บนการ์ด | `Deadline.md` |
| เกณฑ์คุณสมบัติ | ครบ**ก่อน**เริ่มฟอร์ม (ข้อ 01 §4.1) | `01-brand.md §4.1` |

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| ราคาหรือเงื่อนไขซื้อใน accordion | แสดงเลย | ข้อมูลตัดสินใจที่ซ่อนไว้ = คนส่วนใหญ่ไม่เห็น — `Accordion.md §9` |
| สัดส่วนร่วมจ่ายในหน้ารายละเอียดอย่างเดียว | บนการ์ด | เปลี่ยนคำตอบว่า "สมัครได้ไหม" — วงเงิน 500,000 ที่ร่วมจ่าย 50% แปลว่า SME ต้องมีเงินสด 500,000 ของตัวเอง |
| `label="ราคา"` บนวงเงินโครงการ | `"วงเงินสูงสุด"` | ผู้ใช้อ่านเป็นเงินที่จะได้รับ — **เสียหายทางการเงินจริง** |
| ย่อเป็น "วงเงิน" เพราะที่ไม่พอ | เต็มว่า "วงเงินสูงสุด" | คำว่า "สูงสุด" คือทั้งหมดของความหมาย · วัดที่ 136px แล้วอยู่ครบ |
| ให้ผู้ใช้กรอก 4 ขั้นแล้วเจอ "ไม่เข้าเกณฑ์" | แสดงเกณฑ์ครบก่อนเริ่ม | เสียเวลาแล้วค่อยรู้ |

**เมื่อขัดแย้ง** — ชนะทุกข้อในชั้น B · แพ้ทุกข้อในชั้น A

> ตัวอย่างที่ B1 **แพ้** A2: ที่ 320px เอาทุกอย่างขึ้นการ์ดพร้อมกันไม่ได้จริง → แก้ด้วยการ**เปลี่ยนรูปแบบการนำเสนอ** (สลับแนวตั้ง ย่อ label) **ไม่ใช่การซ่อนข้อมูล** — ถ้าทางออกเดียวคือซ่อน แปลว่าเลือกข้อมูลมาแสดงมากเกินไปตั้งแต่ต้น กลับไปตัดที่ B3

---

## B2 · ชัดเจนกว่าสวยงาม
*Clarity over Decoration*

**หลัก** — เมื่อต้องเลือกระหว่าง "ดูดี" กับ "อ่านแล้วเข้าใจทันที" เลือกข้อสอง ทุกครั้ง · สิ่งที่ไม่ได้สื่อความหมาย ต้องพิสูจน์ว่าตัวเองควรอยู่

**ทำไม** — ผู้ใช้ที่กำลังกรอกเอกสารขอสินเชื่อไม่มีอารมณ์มาเดาว่าไอคอนนี้หมายถึงอะไร และในบริบทภาครัฐ การตกแต่งที่ดูเหมือนตราสัญลักษณ์คือการอ้างสิ่งที่ไม่จริง

**เกณฑ์ตรวจ**

- ไอคอนมี label กำกับเสมอ ยกเว้นไอคอนมาตรฐานสากลจริง (ค้นหา ปิด ย้อนกลับ)
- ปุ่มบอกสิ่งที่จะเกิดขึ้น ไม่ใช่ "ตกลง" / "ดำเนินการต่อ"
- ห้ามใช้สัญลักษณ์ที่สื่อการรับรองกับสิ่งที่ไม่มีใครรับรอง
- ทองใช้ ~3% ของหน้า และ **ทองไม่ใช่สถานะ** — ทองกับเหลืองเตือนห่างกันเพียง **1.43:1** ทางความสว่าง
- ไม่มี dark pattern: ไม่มีนับถอยหลังปลอม ไม่มี "เหลืออีก 2 ที่" ที่ไม่จริง
- `alt` ที่ซ้ำกับหัวข้อข้าง ๆ ใช้ `alt=""`

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| ไอคอนโล่/ดาวข้างชื่อใบรับรอง | `showIcon={false}` | อ้างการรับรองด้วยสัญลักษณ์ที่ไม่มีใครรับรอง — `ProductCard.md §9` |
| ปุ่มทองบนทุกการ์ดในหน้ารวม | ทองเฉพาะ CTA แหล่งทุน | ทองหลายใบแย่งกัน = ไม่มีอะไรเด่น — `GrantCard.md §9` |
| `hideClose` เพื่อความสวย | ปล่อยปุ่มปิดไว้ | ผู้ใช้ touch ที่ไม่รู้ว่ากดนอกกล่องได้จะติดค้าง — `Dialog.md §9` |
| `alt={p.name}` ใต้หัวข้อชื่อเดียวกัน | `alt=""` | ชื่ออยู่ในหัวข้อแล้ว — อ่านซ้ำสองรอบ |
| ใช้ `::before` เป็นช่องทางความหมาย | ประกาศใน DOM จริง | `::before` เป็นการตกแต่งล้วน — `Compare.md §5` |

**เมื่อขัดแย้ง** — แพ้ B1 · ชนะ B3 และ B4

---

## B3 · ลดภาระความคิด
*Reduce Cognitive Load*

**หลัก** — สิ่งที่เหลือหลังจาก B1 คัดแล้ว ต้องถูก**ตัดทิ้ง** จัดกลุ่ม หรือทำให้ไม่ต้องจำ · ผู้ใช้ไม่ควรต้องเก็บอะไรไว้ในหัวเพื่อใช้หน้าถัดไป

**ทำไม** — เจ้าของ SME ที่เปิดหน้านี้กำลังทำอย่างอื่นอยู่ด้วย เขาไม่ได้ให้ความสนใจเต็มร้อย และหน้าจอที่ต้องจำค่าจากขั้นก่อนคือหน้าจอที่คนกรอกผิด

**เกณฑ์ตรวจ**

- **จำได้ ดีกว่าต้องนึกออก** (ข้อ 01 §4.3) — ตัวกรองที่เลือกไว้เห็นค้างตลอด ไม่ยุบเป็น "ตัวกรอง (3)"
- จำกัดจำนวนสิ่งที่เทียบพร้อมกัน — **4 รายการ** ไม่ใช่ 8 เพราะคอลัมน์แคบเกินกว่าจะอ่าน
- CTA หลัก **1 ตัวต่อ 1 หน้าจอ** · น้ำเงิน ~12% ของหน้า เกินกว่านั้นแปลว่ามี CTA มากเกินไป
- ฟอร์มยาวต้องเห็นว่าอยู่ขั้นไหนของทั้งหมด · มี `aria-current="step"` **หนึ่งอันเท่านั้น**
- จัดกลุ่มตามสิ่งที่ผู้ใช้กำลังเปรียบเทียบ ไม่ใช่ตามโครงสร้างข้อมูล
- ภาษาคน ไม่ใช่ภาษาราชการ (ข้อ 01 §4.5)

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| ปิดกลุ่มอื่นอัตโนมัติในตัวกรอง | `allowsMultipleExpanded` | ผู้ใช้ต้องจำว่าเลือกอะไรไว้ (ข้อ 01 §4.3) — `Accordion.md §9` |
| ให้เทียบ 8 รายการ | จำกัด 4 | คอลัมน์แคบเกินกว่าจะอ่าน — `Compare.md §9` |
| แนวตั้งจัดกลุ่มตามสินค้า | จัดกลุ่มตามคุณสมบัติ | เสียความสามารถในการเทียบ |
| บัตรเครดิตเป็นตัวเลือกแรก | พร้อมเพย์ก่อน | SME ไทยจำนวนมากไม่มีบัตรนิติบุคคล — `Payment.md §9` |
| `headingLevel={2}` ทุกที่ | ตรงกับลำดับจริง | ลำดับหัวข้อกระโดด = โครงหน้าอ่านไม่รู้เรื่อง |
| แถบเปล่าเมื่อไม่มีรายการ | `return null` | กินพื้นที่ก้นจอโดยไม่ให้อะไร — `Compare.md §9` |

**เมื่อขัดแย้ง** — แพ้ B1 และ B2 · ชนะ B4

> ⚠️ **B3 ไม่ใช่ใบอนุญาตให้ซ่อน** — "ลดภาระ" ทำได้ 3 ทางเรียงตามลำดับที่ควรลอง: **ตัดทิ้ง → จัดกลุ่ม → ค่อยซ่อน** การกระโดดไปข้อสามทันทีคือ B4 ซึ่งอยู่ล่างสุดด้วยเหตุผล

---

## B4 · เปิดเผยตามลำดับ
*Progressive Disclosure*

**หลัก** — **ซ่อนได้ก็ต่อเมื่อไม่ขัด B1, B2 และ B3** ข้อนี้อยู่ล่างสุดโดยเจตนา

**ทำไม** — Progressive disclosure เป็นเครื่องมือที่ถูกใช้ผิดบ่อยที่สุด เพราะมันแก้ปัญหา "หน้ารก" ได้เร็วโดยไม่ต้องตัดสินใจว่าอะไรสำคัญ ผลคือข้อมูลที่ต้องใช้ตัดสินใจไปอยู่หลังคลิกที่คนส่วนใหญ่ไม่กด — `Accordion.md §1` เขียนไว้ตรง ๆ ว่า **"ซ่อนไว้ = คนส่วนใหญ่ไม่เปิด"**

**เกณฑ์ตรวจ — สิ่งที่ซ่อนได้ต้องผ่านทั้ง 4 ข้อ**

1. ไม่เปลี่ยนคำตอบว่าซื้อ/สมัคร/อนุมัติไหม (ผ่าน B1)
2. ผู้ใช้ **ส่วนน้อย** ต้องการ — ถ้าทุกคนต้องอ่าน แสดงเลย
3. มีป้ายที่บอกว่าข้างในคืออะไร ไม่ใช่ "ดูเพิ่มเติม"
4. เข้าถึงได้ด้วย touch และคีย์บอร์ด — **ห้ามใช้ tooltip เป็นที่ซ่อน** (SC 1.4.13)

และ

- `<Accordion>` ที่มี item เดียวไม่ใช่ accordion — ใช้ `<Disclosure>` หรือแสดงเลย
- เป้ากดคือ**ทั้งแถว** (`Accordion.md §5` วัดได้ 56px สูง) ไม่ใช่แค่ไอคอนลูกศร
- `overflow-hidden` ต้องคืนเป็น `visible` ที่ `transitionend` ไม่งั้นตัดวงแหวน focus 4px ใน panel (SC 2.4.7)

**ตารางตัดสิน**

| ❌ | ✅ | ทำไม |
|---|---|---|
| เนื้อหาที่ผู้ใช้ต้องอ่านทุกคน ใส่ accordion | แสดงเลย | ซ่อนไว้ = คนส่วนใหญ่ไม่เปิด — `Accordion.md §1` |
| ราคาหรือเงื่อนไขซื้อใน accordion | แสดงเลย | ขัด B1 ซึ่งชนะข้อนี้ |
| `<Accordion>` ที่มี item เดียว | `<Disclosure>` หรือแสดงเลย | กลุ่มที่มีสมาชิกเดียวไม่ใช่กลุ่ม |
| ซ่อนข้อมูลเสริมใน tooltip | ใน `meta` ที่แสดงอยู่ | ผู้ใช้ touch เข้าไม่ถึง (SC 1.4.13) |
| ซ่อนเพื่อให้หน้าดูสั้นลง | ตัดทิ้ง หรือจัดกลุ่มก่อน | ขัด B3 ซึ่งชนะข้อนี้ |

**เมื่อขัดแย้ง** — แพ้ทุกข้อ · ข้อนี้ไม่เคยชนะอะไร มันคือสิ่งที่**เหลือให้ทำ**หลังจาก B1–B3 ตัดสินแล้ว

---

# วิธีใช้ในการรีวิว

ก๊อปวางใน PR

```markdown
## Design Principles

- [ ] A1 Accessibility — contrast วัดจริง · คีย์บอร์ดครบ · 24×24 · 320px + 200% ไม่มี h-scroll · jest-axe ผ่าน
- [ ] A2 Mobile-Complete — ไม่มีงานที่มือถือทำไม่ได้ · ทดสอบ 320px และ 1280px · role ประกาศครบเมื่อ layout สลับ
- [ ] A3 Consistency — `validate-tokens.js` ผ่าน 11 ข้อ · lint contract ผ่าน · ไม่มี HEX/px/ms ตรง ๆ
- [ ] A4 Reliability (ก) — §4 แจงครบ 8 state หรือประกาศชัดว่าแสดงผลล้วน · async มี loading + error
- [ ] A4 Reliability (ข) — ย้อนกลับไม่ได้ต้องยืนยันด้วยชื่อ/ตัวเลขจริง · ไม่มี optimistic UI กับเงิน
- [ ] A5 Slow Devices — ไม่มี `backdrop-filter` · animation ไม่ผูกกับขนาดเนื้อหา · reduced-motion ทำงาน
- [ ] B1 Fast Decision — ข้อมูลที่เปลี่ยนคำตอบ "ซื้อไหม" เห็นโดยไม่ต้องคลิก
- [ ] B2 Clarity — ไม่มีสัญลักษณ์ที่อ้างสิ่งที่ไม่จริง · ทอง ≤ 3% · ปุ่มบอกสิ่งที่จะเกิด
- [ ] B3 Cognitive Load — ตัวกรองเห็นค้าง · CTA หลัก 1 ตัว · ไม่ต้องจำค่าจากขั้นก่อน
- [ ] B4 Disclosure — สิ่งที่ซ่อนผ่านครบ 4 เงื่อนไข · ไม่มี tooltip เป็นที่ซ่อน
```

**เมื่อสองข้อขัดกัน** เขียนไว้ใน §9 ของ component ว่าข้อไหนชนะและทำไม อย่าตัดสินเงียบ ๆ

---

# ช่องว่างที่รู้ตัว

เกณฑ์ในเอกสารนี้เขียนขึ้นหลัง 03-components สร้างเสร็จ จึงมีเคสที่ยังไม่ผ่าน — **บันทึกไว้ ไม่แก้ในรอบนี้**

| # | ช่องว่าง | ขัดข้อ | สถานะ |
|---|---|---|---|
| 1 | **ไม่มีไฟล์ใดใน `marketplace/` แจงครบ 8 state** — `Accordion.md` มากสุดที่ 5 (collapsed · expanded · hover · focus-visible · disabled) · `Dialog.md` มี 3 (entering · open · exiting) · `Compare.md` มีแค่ empty + disabled | A4 (ก) | ต้องไล่เติม §4 · ยังไม่เริ่ม |
| 2 | **ไม่มีไฟล์ใดใน 8 ไฟล์ที่ตรวจ documented loading/skeleton state** ทั้งที่มี `Skeleton.tsx` อยู่ในระบบ | A4 (ก) | **ปิดเฉพาะเส้นทางซื้อแล้ว** — `CheckoutSummary` มี `onSubmit` / `isSubmitting` / `errorMessage` พร้อม §4 · `BuyBox` และ `Cart` documented ครบ · ที่เหลือยังค้าง |
| 3 | **`GrantCard.md §4` ทั้งหัวข้อเป็นบรรทัดเดียว** — *"สืบทอดจาก `EntityCard`"* โดยไม่บอกว่าครบข้อใด | A4 (ก) | เกณฑ์ใหม่ต้องการรายการที่สืบทอดมา ไม่ใช่ pointer |
| 4 | **มีแค่ `Payment.md` ที่ documented error state ใน §4** | A4 (ก) | — |
| 5 | **`01-brand.md §4.6` เดิมเขียน "360px มาก่อน"** ซึ่ง `Compare.md` ไม่ได้ทำตาม (ออกแบบจากตารางเดสก์ท็อป) | A2 | แก้แล้วในรอบนี้ — ดู 📌 ใน A2 |
| 6 | **ไอคอนโดเมนไทย 14 ตัว** (มอก. · ฮาลาล · GMP · DBD · ThaID · PromptPay) ยังไม่วาด | B2 | ใช้ข้อความแทน · ห้ามหยิบ Lucide ที่ใกล้เคียง (`STYLE-GUIDELINE.md §8`) |
| 7 | **React Aria ไม่มี `th-TH`** — ผู้ใช้ TalkBack ไทยได้ยินอังกฤษเมื่อ RAC ประกาศเอง | A1 | คำแปลพร้อมที่ `strings-rac.th.ts` · วิธีต่อเข้า runtime ยังไม่ตัดสิน |
| 8 | **ทองกับเหลืองห่างกัน 1.43:1** | B2 | ลดความเสี่ยงด้วยกฎ "ทองไม่ใช่สถานะ" · สีอย่างเดียวไม่พอ |
| 9 | ~~**`EntityAmount` ตัวเลข 7 หลักล้นกล่องที่ 136px**~~ | A2 | **แก้แล้ว** — container query ลดตัวเลขเป็น `text-subtitle` เมื่อกล่องแคบกว่า 7rem · `test.fail()` ใน `tests/e2e/pass4.spec.ts` ถอดเป็นเทสปกติแล้ว |
| 10 | **UI ของกระบวนการจับคู่ธุรกิจ** — ส่งคำขอ · ตอบรับ/ปฏิเสธ · นัดหมายเจรจา | — | `BusinessCard` ครอบคลุมแค่การ*แสดง*คู่ค้าที่เสนอ · ส่วนที่เหลือรอ `Tabs` (Pass 4) และ `DataTable` (Pass 5) |
| 11 | **`ServiceCard` มีข้อความ `"บริการ"` hardcode ใน `.tsx`** ไม่ได้มาจาก `strings.th.ts` | A3 | หนี้เดิม พบระหว่างเฟส 2 · แก้เมื่อแตะไฟล์นี้ครั้งถัดไป |

---

*หลักการในเอกสารนี้ต่อยอดจาก [`01-foundations/01-brand.md`](01-foundations/01-brand.md) §4 UX Principles และ §5 Accessibility Principles ซึ่งยังเป็นเจ้าของรายละเอียดระดับปฏิบัติ · เอกสารนี้เป็นเจ้าของ **ลำดับแพ้ชนะ** เท่านั้น*

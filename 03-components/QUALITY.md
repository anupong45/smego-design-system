# Quality Checklist — สรุปหนี้

**`@smego/ui`** · ชั้น 03 · สร้างจาก `## 10 · Quality Checklist` ของทั้ง **50 ไฟล์**

---

## 1 · ภาพรวม

ทุกไฟล์ `.md` ที่คู่กับ `.tsx` มี §Quality Checklist ที่ตรวจ **11 ข้อ** เหมือนกันทุกไฟล์ ไฟล์นี้เก็บเฉพาะ **สิ่งที่ยังไม่ผ่าน** ไว้ที่เดียว เพื่อไม่ต้องเปิดอ่านทุกไฟล์เพื่อหาว่าเหลืออะไร

**บังคับด้วย `lint:docs` แล้ว** (2026-07-29) — ไม่มี checklist · แถวไม่ครบ 11 · หรือลำดับรายการไม่ตรง = เกตแดง

> ⚠️ ก่อนมีเกต คำอ้างในย่อหน้าข้างบน**ไม่จริง**: วัดได้ **59 จาก 62 ไฟล์**
> `Icon.md` ไม่มี checklist เลย · `Main.md` ยุบ 4 แถวเป็นแถวเดียว ·
> `TextArea.md` ใช้ checklist ชุดเก่า 4 เกณฑ์คนละชุด
> สองไฟล์แรกถูกเขียนขึ้นในสัปดาห์เดียวกับที่กฎนี้ถูกเขียนลงไป
> และตัวเลขรวมด้านล่างก็เคยค้างอยู่ที่ "50 ไฟล์ = 550 แถว" ทั้งที่มี 62 ไฟล์

| ผล | จำนวน | ความหมาย |
|---|---|---|
| ✅ | **456** | มีหลักฐานรองรับ — ชื่อเทสที่รันได้ หรือค่าที่วัดจริง |
| ⚠️ | **0** | — หนี้ทั้ง 5 ปิดครบเมื่อ 2026-07-29 (ดู §2) |
| ❌ | **0** | — |
| — | **226** | ไม่เกี่ยวข้องกับ component นั้น **พร้อมเหตุผลกำกับทุกแถว** |
| **รวม** | **682** | 62 ไฟล์ × 11 ข้อ · นับจริงเมื่อ 2026-07-29 |

### ทำไม `—` ถึงมีเยอะ และทำไมนั่นถูกแล้ว

`—` ไม่ใช่การเว้นว่าง — ทุกแถวต้องเขียนเหตุผล เพราะ **N/A ที่ไม่มีเหตุผลคือที่ซ่อนที่ง่ายที่สุดของช่องโหว่จริง**

ส่วนใหญ่มาจากสองกลุ่มที่ตั้งใจไม่มีสถานะเหล่านั้น:

- **การ์ด 7 ใบที่สืบทอด [`EntityCard`](./src/marketplace/EntityCard.md)** — §4 ของ [`FundingCard`](./src/marketplace/FundingCard.md) และ [`BusinessCard`](./src/marketplace/BusinessCard.md) **ประกาศไว้ตรง ๆ** ว่าไม่มี `disabled`/`loading`/`error`/`empty` พร้อมเหตุผล ตาม `DESIGN-PRINCIPLES A4(ก)` — สถานะเหล่านั้นเป็นของกริดที่ครอบอยู่ ไม่ใช่ของการ์ดแต่ละใบ
- **ตัวควบคุมในฟอร์ม** — ช่องกรอกไม่มี "ว่างเปล่า" ที่ต้องออกแบบ (ช่องว่างคือสถานะปกติ) และไม่มี "กำลังโหลด" (นั่นเป็นของปุ่มส่ง)

---

## 2 · หนี้ทั้ง 5 รายการ — **ปิดครบเมื่อ 2026-07-29**

| # | หนี้เดิม | ปิดด้วย |
|---|---|---|
| 2.1 | `Tooltip` ยังไม่ได้วัดที่ 320px | **วัดแล้ว ไม่ต้องแก้** — `e2e/tooltip-320.spec.ts`: กล่อง **256px** วางที่ **12–268** · `scrollWidth 320 = clientWidth` · การวางตำแหน่งอัตโนมัติของ RAC เอาอยู่จริง จึงไม่ต้องจำกัดความกว้างเพิ่มและไม่ต้องจำกัดความยาวข้อความ |
| 2.2 | `FileInput` ไม่มีสถานะ "กำลังอัปโหลด" | **ปิดเพราะนอกขอบเขต** — `isLoading` ของ Astryx อยู่นอกขอบเขต parity ตามคำตัดสิน 2026-07-28 ข้อ 1 · หนี้ข้อนี้ขอสิ่งที่ scope ถอนไปแล้ว (รูปแบบเดียวกับที่ `Compare.md` เคยสัญญา row selection) |
| 2.3 | `Payment`/`SlipUpload` หนี้เดียวกับ 2.2 | ปิดตาม 2.2 |
| 2.4 | `Deadline` ไม่มีเทสของตัวเอง | **`a11y/deadline.test.tsx` 9 ข้อ** — ล็อกว่า `dateTime` เก็บ ค.ศ. ขณะข้อความเป็น พ.ศ. (สลับกันคือผิด **543 ปี** แบบที่ตามองไม่เห็น) · เขตเวลา Asia/Bangkok ไม่ทำให้วันเลื่อน · ทุกสถานะอ่านต่างกันได้โดยไม่เห็นสี |
| 2.5 | `OTPField` `h-12` ตายตัว | **เปลี่ยนเป็น `min-h-12`** — ยังได้พื้น 48px เท่าเดิม แต่ยืดตามได้เมื่อผู้ใช้ตั้งขนาดตัวอักษรถึง 200% (SC 1.4.4) |

**นับใหม่จากของจริง:** ✅ **456** · ⚠️ **0** · ❌ 0 · — **226** = **682**

> ⚠️ **สิ่งที่พบตอนปิดหนี้ และควรอ่าน**
>
> · **หนี้ 2.2 ขัดกับคำตัดสินของระบบเอง** — มันขอสถานะอัปโหลดซึ่งแปลว่าต้องรับ `isLoading` แต่คำตัดสิน 2026-07-28 ข้อ 1 ประกาศว่า prop นอกชุด §8 อยู่นอกขอบเขต · หนี้ที่ขอสิ่งที่ scope ถอนแล้วจะค้างตลอดไปเพราะไม่มีใครปิดได้
> · **`FileInput.tsx` หัวไฟล์เคยเขียนว่า "ไม่รับ `status`"** ทั้งที่ `status` ถูกเพิ่มเข้ามาเมื่อ 2026-07-29 · ค้างอยู่หนึ่งวัน · **ไม่มีเกตไหนตรวจคอมเมนต์ใน `.tsx`** — `lint-docs` ดูแค่ `.md`
> · **`gallery` เองเลื่อนแนวนอนที่ 320px** (`scrollWidth 549`) · เทส 2.1 จึงต้องวัดบน `fixture` ไม่ใช่ gallery · guard ที่เช็ค "หน้าเลื่อนอยู่แล้วหรือเปล่า" **ก่อน** เปิด tooltip เป็นตัวที่กันการรายงานผิดคน
>
> ✅ **แก้แล้ว 2026-07-30 — และต้นเหตุไม่ใช่ `Table` อย่างที่เดาไว้ตรงนี้**
> ① `Group` ของ gallery เป็น `grid` ที่ไม่ประกาศคอลัมน์ ⇒ implicit track `auto`
> ถูกดันไป 540px โดย specimen ที่ max-content กว้าง (556 → 443)
> ② ★★ **บั๊กของ component** — `sr-only` (`position: absolute`) ของ `CategoryNav`
> อยู่ในกล่องที่เลื่อนแนวนอนแต่ **ไม่มี positioned ancestor** ⇒ หลุดไปอยู่ที่ x=442
> และดันความกว้างของ `html` · ทั้งระบบมี scroll container **14 จุด** และตอนนั้น
> **ไม่มีจุดไหนเป็น `relative`** ⇒ ทุกแอปที่ใช้ `CategoryNav` แบบ scroll จะเจอ
> หน้าเลื่อนแนวนอน = **SC 1.4.10 แดง** (443 → 320)
> เติม `relative` 10 จุด + กฎ `lint:quality` ข้อ `scroll` + `e2e/reflow-320.spec.ts`

---

## 3 · เครื่องมือ

```bash
npm run lint          # lint-classes (สี) + lint-quality (4 ข้อ) + parity + docs
npm run lint:quality  # เฉพาะ 4 ข้อ
npm run lint:docs     # ลิงก์เสีย + ชื่อก่อน rename ในเอกสาร
npm run verify        # ทั้งหมด: typecheck → lint → vitest → playwright → tokens
```

### bundle budget (ปิดหนี้จากรอบ grill 2026-07-26 · ทำเมื่อ 2026-07-29)

รอบนั้นตัดสินว่า **"per-page bundle budget ใน `verify` + lazy-load 3 ตัว"**
ของจริงก่อนวันนี้: `index.ts:27` มีแต่ **คอมเมนต์แนะนำ** ให้ lazy-load
ไม่มีเกต และไม่มี `lazy()` ที่ไหนเลย — เจตนาที่ไม่มีหลักฐาน รูปเดียวกับ
contrast sweep ที่ค้างอยู่สามวัน

เกตอยู่ที่ [`scripts/check-bundle.mjs`](./scripts/check-bundle.mjs) แล้ว
วัด gzip ของ entry จำลองสามรูปแบบหน้า:

| หน้า | วัดได้ (2026-07-30) | เพดาน |
|---|---|---|
| listing — รายการสินค้า | 44.5 KB | 50 |
| **form — ขอสินเชื่อ** | **142.5 KB** | 148 |
| full — import ทุกตัว | 188.9 KB | 200 |

★ **2026-07-30 เปลี่ยนวิธีวัด** — เดิม `--bundle --outfile` **ไม่มี `--splitting`**
⇒ esbuild ยัด dynamic import กลับเข้าไฟล์เดียว · เกตจึงวัด "ผลรวมของโค้ดที่หน้า
อ้างถึง" แต่ **รายงานเหมือนเป็นสิ่งที่ผู้ใช้ดาวน์โหลด** ตอนนี้ใช้ `--splitting`
+ metafile แยก **initial** (ก่อน first paint · เพดานคุมตัวนี้) จาก **lazy**
พิสูจน์แล้วว่าแยกถูก: module ที่โหลดผ่าน `import()` เท่านั้น เนื้อบีบไม่ได้ 90 KB
→ initial คงที่ 42.9 · lazy 88.2

### 2.6 · หน้า form หนัก 142.5 KB gzip — **ไลบรารีแก้ไม่ได้ · ย้ายเจ้าของไปรีโปแอป**

| | |
|---|---|
| อาการ | หน้าขอสินเชื่อโหลดโค้ด component **142.5 KB gzip** · `DateInput` คิดเป็น **40.2 KB** ของยอดนี้ (102.3 → 142.5) |
| ทำไมสำคัญ | เกินความสบายของเกณฑ์อุปกรณ์ในข้อ 01 (Android ระดับล่าง 4G) ซึ่งเป็นกลุ่มผู้ใช้หลักของแพลตฟอร์มนี้ |
| ★★★ ฝั่งไลบรารี | **ปิดแบบ "ทำไม่ได้" 2026-07-30 — ไม่ใช่ "ทำแล้ว"** · คำอ้างเดิมว่า "ปิดแล้ว 2026-07-29 · พิสูจน์แล้วว่า dynamic import แยก chunk ได้จริง" **จริงแค่ครึ่ง** — แยก chunk ได้จริง แต่ **initial ไม่ลด** |
| หลักฐาน | ทำจริงแล้ววัด metafile: ① ของหนักคือ **`RACDatePicker` เอง ไม่ใช่ `Calendar`** (`Calendar.mjs` = 4.3 KB · ส่วนที่หนักคือ `@internationalized/date` + เลขคณิตวันที่ของ react-stately ที่ `RACDatePicker` ลากมาเอง) ⇒ กำไร ~6 KB ② **หน้ารายการสินค้าโต 44.5 → 67.1 KB** เพราะ esbuild ยก input ที่ทั้ง entry และ chunk lazy เข้าถึงได้ขึ้นไปอยู่ใน shared chunk ที่ entry static import — หน้าที่ไม่มี `DateInput` ต้องแบกโค้ดปฏิทิน ③ lazy **ทั้ง `DateInput`** ก็ไม่ช่วย shared chunk ยัง 137.4 KB เพราะ entry import RAC สำหรับ `TextInput` อยู่แล้ว |
| ฝั่งแอป | **เจ้าของตัวจริง** — 40 KB หายได้เฉพาะเมื่อหน้านั้น **ไม่อ้างถึง `DateInput` เลย** ซึ่งคือ route-level splitting ที่ Next ทำให้เองอยู่แล้ว · ⚠️ webpack chunk ต่างจาก esbuild และ **อาจ** ย้ายออกได้จริง แต่ **พิสูจน์ในรีโปนี้ไม่ได้ จึงไม่อ้าง** — ต้องวัดที่รีโปแอป |
| ห้ามทำซ้ำ | **ห้ามใส่ `lazy()` กลับมาโดยไม่วัดใหม่ทั้งสามหน้า** — ครั้งก่อนมันทำให้หน้าที่ไม่เกี่ยวแย่ลง |
| ⚠️ เจ้าของเดิมหายไป | เคยเขียนว่า "รอ template จริง (layer 05)" แต่คำตัดสิน 2026-07-29 คือ **ไม่ทำชั้น 05** — หนี้ที่ฝากไว้กับชั้นที่ไม่เกิดคือหนี้ที่ไม่มีใครปิด จึงแยกเจ้าของเป็นสองฝั่งตามด้านบน |
| กันไว้อย่างไร | เพดานตั้งชิด (148) **เพื่อกันการโตต่อ ไม่ใช่เพื่อรับรองค่านี้** — หลักเดียวกับเพดาน parity ที่ fail ทั้งตอนเกินและตอนต่ำกว่า |

★ `Table` ที่เพิ่มวันนี้เป็นหลักฐานว่ากลไกนี้ใช้ได้: ใช้ `<table>` ปกติแทน
RAC `Table` ทำให้ full bundle ขยับจาก 186.3 → **186.9 KB** (+0.6) ส่วน RAC
จะเป็น **+44 KB** (ดู [`Table.md` §1](./src/data-display/Table.md))

### contrast sweep (ปิดหนี้จากรอบ grill 2026-07-26)

รอบนั้นสรุปว่าจะทำ sweep อัตโนมัติ "ทุกหน้า preview ทั้งสองโหมด" เพราะบั๊ก
`primary-50` เป็นพื้นหลุดถึงโค้ดพร้อมใช้ **เนื่องจากตรวจ contrast แบบสุ่มจุด**
ทำเสร็จเมื่อ **2026-07-29** ที่ [`tests/e2e/contrast-sweep.spec.ts`](./tests/e2e/contrast-sweep.spec.ts)

| | ครอบ |
|---|---|
| ✅ SC 1.4.3 ข้อความ | **618 element** บนหน้า gallery × 2 โหมด + **32 element** ใน popover ที่เปิด |
| ❌ SC 1.4.11 ขอบเขต UI (3:1) | ยังพึ่ง spec รายตัว (`pass3` · `banner`) — แยก "ขอบสื่อความหมาย" จากขอบตกแต่งด้วย computed style ไม่ได้ |
| ❌ state `hover` / `focus` | ยังไม่ครอบ |

⚠️ **สองบทเรียนจากการเขียน sweep นี้**

1. **ต้องดับ `transition` ก่อนวัด** — ฉบับแรกอ่านสีต่างกันทุกรอบ
   (`rgb(120,123,128)` แล้ว `rgb(124,127,133)`) เพราะวัดตอนสีไล่อยู่กลางทาง
   ให้ "ปัญหา" ปลอม 2 จุด · flaky ยังไม่ร้ายเท่า **รายงานสีผิด** ซึ่งพาไปแก้
   token ที่ไม่ได้ผิด
2. **เกตต้องพิสูจน์ว่า fail ได้** — ฉีดสี 3.45:1 เข้าไปแล้วต้องจับได้ 10 จุด
   ก่อนจะเชื่อว่าที่มันเขียวคือเขียวจริง · sweep ที่ตรวจ 0 element ก็เขียว
   เหมือนกัน จึงมีเกณฑ์ขั้นต่ำ (`checked > 150`) กันการตายเงียบ

ผลตอนนี้: **ไม่มีจุดที่ไม่ผ่าน** ทั้งสองโหมด

[`lint/lint-quality.mjs`](../lint/lint-quality.mjs) เป็นหลักฐานของ 4 ข้อใน checklist:

| ข้อใน checklist | กฎ | ระดับ |
|---|---|---|
| คุณสมบัติเชิงตรรกะ | `ml-`/`pl-`/`left-`/`text-left`/`border-l-`/`rounded-l` | **error** |
| โหมดมืด | `shadow-sm`/`md`/`lg` ดิบแทน `--elevation-*` | **error** |
| ประสิทธิภาพ | `h-9` ขึ้นไปที่ตั้งตายตัว (ไม่นับ `max-h-`/`min-h-`) | warn |
| การเคลื่อนไหว | การเคลื่อนที่ขาเข้า/ขาออกที่ไม่ติดป้าย `data-motion="transform"` | warn |

**ตัวเลือก:** `--strict` ทำให้ warn นับเป็น error · `--json` พ่นผลรายไฟล์

### ข้อยกเว้นสองข้อที่เขียนไว้ในตัว linter

ทั้งคู่เป็น false positive ที่เจอตอนรันจริง และถูกยกเว้น**พร้อมเหตุผล** ไม่ใช่ปิดกฎทิ้ง:

1. **`left-1/2` + `-translate-x-1/2`** ([`Slider.tsx:140`](./src/inputs/Slider.tsx)) — เป็นสำนวนจัดกึ่งกลาง ไม่ได้เลือกข้าง · Tailwind ไม่มี `translate` เชิงตรรกะ การเปลี่ยนเป็น `start-1/2` จะทำให้เยื้องออกนอกกึ่งกลางใน RTL คู่ physical ที่เข้าคู่กันถูกกว่าคู่ที่ปนกันครึ่ง ๆ
2. **`max-h-`/`min-h-`** ([`Selector.tsx:133`](./src/inputs/Selector.tsx) · [`Typeahead.tsx:158`](./src/inputs/Typeahead.tsx)) — เพดานและพื้น ไม่ใช่ความสูงตายตัว · `max-h-64` บน listbox ยังยืดตามเนื้อหาได้เต็มที่ก่อนถึงเพดาน

### ⚠️ บันทึกจากการรันครั้งแรก — กฎการเคลื่อนไหวฉบับแรกผิด

ฉบับแรกฟ้อง `animate-*` ทุกตัวและได้ **12 จุด** (`Dialog` · `Toast` · `Tooltip` · `Typeahead` · `DateInput` · `Selector` · `Skeleton`) — **false positive ทั้งหมด**

[`base.css §10`](../02-tokens/src/base.css) ครอบด้วย `*, *::before, *::after` + `!important` อยู่แล้ว: `animation-duration: 1ms` ตัด keyframe ทุกตัว · `.animate-pulse`/`.animate-bounce`/`.animate-ping` และ `.skeleton` อยู่ในรายการ DENY เจาะจง · และการตั้ง `transition-property` ใหม่ทำให้ `transition-transform` ของ [`Switch`](./src/inputs/Switch.md) กับ [`Collapsible`](./src/data-display/Collapsible.md) หยุดเป็น transition ให้เอง (หัวสวิตช์และลูกศรยัง**ย้ายตำแหน่งทันที** เพราะเป็นตัวบอกสถานะ ไม่ใช่การตกแต่ง)

ช่องโหว่จริงมีข้อเดียว: block `DENY` ที่ตัด transform เป็นแบบ **opt-in** ผูกกับ `[data-motion="transform"]` · `.motion-slide` · `.motion-scale` — การเคลื่อนไหวขาเข้า/ขาออกที่**เลื่อนหรือซูมจริง**จะยังเคลื่อนที่ถ้าไม่ติดป้าย กฎถูกเขียนใหม่ให้จับเฉพาะกรณีนั้น ซึ่งตอนนี้ได้ **0 จุด**

**บทเรียน:** linter ที่ฟ้องของที่ปลอดภัยจะถูกปิดทิ้งภายในสปรินต์เดียว
